# Exclude dependencies and build artifacts from Docker context
node_modules
.pnpm-store
.next
out
.build

# Misc project files not needed inside the image
.git
.gitignore
Dockerfile
README.md

# OS / editor noise
.DS_Store
Thumbs.db
